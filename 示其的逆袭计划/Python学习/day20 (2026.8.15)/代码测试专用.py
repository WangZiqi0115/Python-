# Day 20 · 代码测试专用 (2026.8.15)
# 在这里随便写、随便试，不影响正式练习。

import functools

# # 示例：试试 reduce 求和
# print(functools.reduce(lambda a, b: a + b, [1, 2, 3, 4, 5]))

# # 示例：试试 partial 固定 pow 的底数
# square = functools.partial(pow, 2)
# print(square(3))   # 8

# # 示例：试试 keyword-only
# def order(base, *, sugar="七分糖"):
#     return f"{base}，{sugar}"
# print(order("珍珠奶茶", sugar="无糖"))

print("Day 20 代码测试专用")
