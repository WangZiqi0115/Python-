# Day 25 · NumPy 矩阵乘法与转置 练习 (2026.9.7)
# ============================================
# 先在终端运行一次确认能跑:  python "Day25_NumPy矩阵乘法与转置练习.py"
# 每题下方标注了期望输出,完成后再运行对比。
# 前提: 已 pip install numpy
# 提示: 不确定就回「知识点」看对应的例子,别硬背。


import numpy as np

# ============================================================
# 练习 1 · 矩阵乘法【基础形状】(10 分)
# 下面两个矩阵,判断能不能乘,能乘就 print 结果
# ============================================================
A = np.array([[1, 2, 3],
              [4, 5, 6]])            # (2,3)
B = np.array([[7, 8],
              [9, 10],
              [11, 12]])            # (3,2)
# 请在下方写代码: 用 @ 把 A 和 B 乘起来,print 结果和结果形状
# 期望: [[ 58  64]
#        [139 154]],形状 (2,2)
# 提示: A 有 3 列、B 有 3 行,公共维度数 3 对上了,能乘。
# 请在下方写代码(一行打印"乘出来的结果",另一行打印"结果的形状")



# ============================================================
# 练习 2 · 判断能不能乘(10 分)
# 下面这对,公共维度数对不上,会报错。你的任务是"看"出来,并写一句注释说明原因
# ============================================================
C = np.ones((2, 3))      # (2,3)
D = np.ones((4, 2))      # (4,2)
# (C @ D) 会报错。请在下方用一行注释回答: 为什么报错?
# 提示: 看 C 的列数 和 D 的行数,这两个就是公共维度数。
# 请在下方写注释(把原因写清楚)



# ============================================================
# 练习 3 · @ 和 * 的区别(10 分)
# 对下面这个矩阵,分别打印"矩阵乘它自己"和"逐元素乘它自己",对比区别
# ============================================================
M = np.array([[1, 2],
              [3, 4]])
# 请在下方写两行代码:
#   一行用它和自己做"矩阵乘法"(@) 并打印
#   一行用它和自己做"逐元素乘法"(*) 并打印
# 期望: @ 是 [[ 7 10]
#             [15 22]];* 是 [[ 1  4]
#                            [ 9 16]]
# 请在下方写代码(两行 print)



# ============================================================
# 练习 4 · 转置(10 分)
# 把下面这个 (2,3) 矩阵转置,打印转置结果和转置后的形状
# ============================================================
P = np.array([[1, 2, 3],
              [4, 5, 6]])            # (2,3)
# 请在下方写代码: 用 P.T 转置,print 出来,再用 .shape 看形状
# 期望: [[ 1  4]
#        [ 2  5]
#        [ 3  6]],形状 (3,2)
# 提示: 转置用 `.T`,然后分别打印转置后的数组、以及它的形状。
# 请在下方写代码(一行打印转置结果,一行打印形状)



# ============================================================
# 练习 5 · 转置让维度对齐(10 分)
# 下面这俩能乘吗?试试看;报错的话,用转置 .T 让它们"对得上"再乘
# ============================================================
X = np.random.rand(100, 784)     # 100张图,每张784像素
W = np.random.rand(784, 64)      # 权重:784输入 → 64输出
# 请在下方写代码:
#   (1) 先试 X @ W,print 结果形状(这步能直接乘)
#   (2) 再想: 如果 W 是 (64, 784) 而不是 (784, 64),该怎么调?(用 .T)
# 期望: X @ W 的形状是 (100, 64)
# 提示: 第(1)步直接乘;第(2)步是想让"公共维度数"相等,看 .T 把哪个转一下。
# 请在下方写代码(先跑通 X @ W,再想第(2)问)



# ============================================================
# 练习 6 · 神经网络的前向传播(重点 · 20 分)
# 用矩阵乘法搭出 784 → 64 → 10 的前向传播,看它"猜"哪个数字
# ============================================================
np.random.seed(1)
W1 = np.random.randn(64, 784)    # 输入层→隐藏层权重
b1 = np.random.randn(64)         # 隐藏层偏置
W2 = np.random.randn(10, 64)     # 隐藏层→输出层权重
b2 = np.random.randn(10)         # 输出层偏置
x = np.random.rand(784)          # 一张图(784个像素)

# 请在下方写代码: 用一次矩阵乘法算出隐藏层(再把偏置加上),存进变量,并打印它的形状
# 期望: (64,)  —— 因为 W1(64,784) 和 x(784,) 公共维度数 784 对上 → 得到 64 个数
# 提示: 先想"神经元得分 = (权重 @ 输入) + 偏置",把结果存进一个变量,再打印这个变量的 .shape。
# 请在下方写代码(想清楚该把什么存进变量,再打印它的形状)



# ============================================================
# 练习 7 · 回顾题(回顾 Day 21 的异常进阶)
# ============================================================
# 复习 Day 21:try/except/else/finally 四兄弟、raise、assert。
# 下面这段代码,运行后【输出顺序】是怎样的?(想想 finally 什么时候跑)
# 任务1:说明这段代码会打印什么、按什么顺序
# 任务2:用一句话解释为什么 finally 的打印会先于函数 return 的值被外层看到
# 请在下方写代码(可以先跑一下验证,再看问题)
def demo(a, b):
    try:
        result = a / b
    except ZeroDivisionError:
        return "出错了: 除以0"
    else:
        print(f"成功: {a}/{b} = {result}")
        return result
    finally:
        print("收尾: 无论如何都会执行")

print(demo(10, 2))
print(demo(1, 0))
# 请在下方写答案(注释形式):
# 任务1 输出顺序:
#
# 任务2 为什么 finally 先于 return:
#



# ============================================================
# 检验标准(今天的「完成」标准)
# ============================================================
# [ ] 会用 A @ B 做矩阵乘法,会看形状 (行A×公共维度数)@(公共维度数×列B)=(行A×列B)
# [ ] 看得出公共维度数不匹配会报错,知道原因
# [ ] 分得清 @(矩阵乘) 和 *(逐元素乘),不会用错
# [ ] 会用 A.T 转置,并用来对齐维度
# [ ] 会写前向传播 W @ x + b,明白 W1(64,784) @ x(784,) → (64,)
# [ ] 回顾题: 记得 finally 先于 return、else 只在成功时跑、except 按顺序匹配
#
# 全对 → Day 25 完成 🎉
